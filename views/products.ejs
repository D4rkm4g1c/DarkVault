<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - Products</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2, h3 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .product-card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        form {
            margin-top: 20px;
        }
        .form-row {
            display: flex;
            margin-bottom: 15px;
            align-items: center;
        }
        label {
            width: 100px;
            margin-right: 10px;
        }
        input[type="text"], input[type="number"] {
            padding: 8px;
            background-color: #333;
            color: #fff;
            border: 1px solid #444;
            border-radius: 3px;
            flex: 1;
        }
        button {
            background-color: #00aaff;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 3px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0088cc;
        }
        .result-box {
            background-color: #333;
            border: 1px solid #444;
            border-radius: 3px;
            padding: 20px;
            margin-top: 20px;
        }
        .no-results {
            color: #ff6666;
        }
        .results-found {
            color: #66ff66;
        }
        .info-text {
            margin-top: 15px;
            font-size: 0.9em;
            color: #aaa;
        }
        .quick-links {
            margin-top: 20px;
            margin-bottom: 20px;
        }
        .quick-links a {
            display: inline-block;
            background-color: #333;
            color: #00aaff;
            text-decoration: none;
            padding: 8px 15px;
            border-radius: 3px;
            margin-right: 10px;
            transition: background-color 0.2s;
        }
        .quick-links a:hover {
            background-color: #444;
        }
        .filter-badge {
            display: inline-block;
            background-color: #00aaff;
            color: white;
            padding: 3px 8px;
            border-radius: 10px;
            font-size: 0.8em;
            margin-left: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Product Search</h1>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/settings">Settings</a>
                <a href="/contact">Contact</a>
                <% if (user.isAdmin) { %>
                    <a href="/admin">Admin Panel</a>
                <% } %>
                <a href="/logout">Logout</a>
            </div>
        </header>
        
        <div class="quick-links">
            <a href="/products">New Search</a>
            <a href="/settings">Manage Saved Filters</a>
        </div>
        
        <div class="product-card">
            <h2>Search Products</h2>
            <p>Use the form below to search our product database.</p>
            
            <form action="/products/search" method="POST">
                <div class="form-row">
                    <label for="category">Category:</label>
                    <input type="text" id="category" name="category" placeholder="Enter product category (e.g., Electronics, Audio)">
                </div>
                
                <div class="form-row">
                    <label for="minPrice">Min Price:</label>
                    <input type="number" id="minPrice" name="minPrice" min="0" step="0.01" placeholder="Minimum price">
                </div>
                
                <div class="form-row">
                    <label for="maxPrice">Max Price:</label>
                    <input type="number" id="maxPrice" name="maxPrice" min="0" step="0.01" placeholder="Maximum price">
                </div>
                
                <button type="submit">Search Products</button>
            </form>
            
            <div class="info-text">
                <p>Example categories: Electronics, Audio, Accessories, Storage</p>
                <p>Pro tip: You can save your searches as filters in your <a href="/settings" style="color: #00aaff;">settings page</a>.</p>
            </div>
            
            <% if (searchPerformed) { %>
                <div class="result-box">
                    <% if (searchParams.filterName) { %>
                        <h3>Filter Results: <span class="filter-badge"><%= searchParams.filterName %></span></h3>
                    <% } else { %>
                        <h3>Search Results</h3>
                    <% } %>
                    
                    <% if (hasResults) { %>
                        <p class="results-found">Products matching your criteria were found!</p>
                        <p>For security reasons, we only show whether products exist or not. Contact an administrator for more details.</p>
                    <% } else { %>
                        <p class="no-results">No products match your search criteria.</p>
                    <% } %>
                    
                    <p><strong>Your search:</strong> 
                        <% if (searchParams.category) { %>Category: "<%= searchParams.category %>"<% } %>
                        <% if (searchParams.minPrice) { %> Min Price: $<%= searchParams.minPrice %><% } %>
                        <% if (searchParams.maxPrice) { %> Max Price: $<%= searchParams.maxPrice %><% } %>
                    </p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html> 