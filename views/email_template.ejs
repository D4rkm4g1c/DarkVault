<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - Email Templates</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2, h3 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        form {
            margin-top: 15px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        textarea {
            width: 100%;
            height: 200px;
            padding: 8px;
            background-color: #333;
            color: #fff;
            border: 1px solid #444;
            border-radius: 3px;
            font-family: monospace;
        }
        button {
            background-color: #00aaff;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 3px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0088cc;
        }
        .error-message {
            background-color: rgba(255, 0, 0, 0.1);
            border: 1px solid #aa0000;
            color: #ff6666;
            padding: 10px;
            border-radius: 3px;
            margin-bottom: 20px;
        }
        .preview-container {
            margin-top: 20px;
            background-color: #333;
            border: 1px solid #444;
            border-radius: 3px;
            padding: 20px;
        }
        .preview-header {
            margin-bottom: 10px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        .preview-content {
            white-space: pre-wrap;
            font-family: Arial, sans-serif;
            padding: 15px;
            background-color: #fff;
            color: #000;
            border-radius: 3px;
        }
        .examples {
            margin-top: 15px;
            font-size: 0.9em;
            color: #aaa;
            line-height: 1.5;
        }
        .code-examples {
            background-color: #222;
            padding: 10px;
            border-radius: 3px;
            font-family: monospace;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Email Template Editor</h1>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/settings">Settings</a>
                <a href="/products">Products</a>
                <a href="/preview">URL Preview</a>
                <a href="/import">Import Products</a>
                <a href="/contact">Contact</a>
                <% if (user.isAdmin) { %>
                    <a href="/admin">Admin Panel</a>
                <% } %>
                <a href="/logout">Logout</a>
            </div>
        </header>
        
        <% if (error) { %>
            <div class="error-message">
                <p><%= error %></p>
            </div>
        <% } %>
        
        <div class="card">
            <h2>Create Email Template</h2>
            <p>Design custom email templates with EJS syntax. These templates will be sent to users.</p>
            
            <form action="/email-template" method="POST">
                <div class="form-group">
                    <label for="template">Template Content:</label>
                    <textarea id="template" name="template" required><%= template %></textarea>
                </div>
                
                <button type="submit">Preview Template</button>
            </form>
            
            <div class="examples">
                <p><strong>Available Variables:</strong></p>
                <ul>
                    <li><code>username</code>: The user's username</li>
                    <li><code>email</code>: The user's email address</li>
                </ul>
                
                <p><strong>Example Templates:</strong></p>
                <div class="code-examples">
Hello <%= username %>,

Thank you for signing up to DarkVault! 
Your account has been created with the email: <%= email %>.

Best regards,
The DarkVault Team
                </div>
            </div>
        </div>
        
        <% if (preview) { %>
            <div class="preview-container">
                <div class="preview-header">
                    <h3>Template Preview</h3>
                </div>
                
                <div class="preview-content">
<%= preview %>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html> 