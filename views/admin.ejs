<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 1100px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .admin-card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            padding: 10px;
            text-align: left;
            border-bottom: 1px solid #444;
        }
        th {
            background-color: #333;
        }
        tr:hover {
            background-color: #333;
        }
        .action-btn {
            background-color: #00aaff;
            color: white;
            border: none;
            padding: 5px 10px;
            border-radius: 3px;
            cursor: pointer;
            margin-right: 5px;
            text-decoration: none;
            display: inline-block;
        }
        .action-btn:hover {
            background-color: #0088cc;
        }
        .delete-btn {
            background-color: #ff5555;
        }
        .delete-btn:hover {
            background-color: #dd3333;
        }
        .edit-btn {
            background-color: #ffa500;
        }
        .edit-btn:hover {
            background-color: #cc8400;
        }
        .admin-links {
            display: flex;
            gap: 10px;
            margin: 20px 0;
        }
        .admin-links a {
            background-color: #00aaff;
            color: white;
            text-decoration: none;
            padding: 8px 15px;
            border-radius: 3px;
            transition: background-color 0.2s;
        }
        .admin-links a:hover {
            background-color: #0088cc;
        }
        .add-user-form {
            background-color: #333;
            padding: 15px;
            border-radius: 5px;
            margin-top: 20px;
            display: none;
        }
        .form-row {
            display: flex;
            margin-bottom: 10px;
            gap: 15px;
        }
        .form-group {
            flex: 1;
        }
        .form-group label {
            display: block;
            margin-bottom: 5px;
        }
        .form-group input, .form-group select {
            width: 100%;
            padding: 8px;
            background-color: #444;
            border: none;
            border-radius: 3px;
            color: #fff;
        }
        .alert {
            padding: 10px;
            margin-bottom: 15px;
            border-radius: 3px;
        }
        .alert-success {
            background-color: rgba(40, 167, 69, 0.2);
            border: 1px solid #28a745;
            color: #28a745;
        }
        .alert-danger {
            background-color: rgba(220, 53, 69, 0.2);
            border: 1px solid #dc3545;
            color: #dc3545;
        }
        .user-edit-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.7);
            z-index: 1000;
        }
        .modal-content {
            position: relative;
            background-color: #2a2a2a;
            margin: 10% auto;
            padding: 20px;
            width: 50%;
            border-radius: 5px;
        }
        .close-modal {
            position: absolute;
            top: 10px;
            right: 15px;
            color: #aaa;
            font-size: 28px;
            font-weight: bold;
            cursor: pointer;
        }
        .close-modal:hover {
            color: #fff;
        }
        .settings-saved {
            display: none;
            background-color: rgba(40, 167, 69, 0.2);
            border: 1px solid #28a745;
            color: #28a745;
            padding: 10px;
            border-radius: 3px;
            margin-top: 10px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Admin Panel</h1>
            <div class="nav-links">
                <a href="/dashboard"><i class="bi bi-speedometer2"></i> Dashboard</a>
                <a href="/products"><i class="bi bi-box"></i> Products</a>
                <a href="/contact"><i class="bi bi-envelope"></i> Contact</a>
                <a href="/ping"><i class="bi bi-hdd-network"></i> Network Tools</a>
                <a href="/api/docs"><i class="bi bi-code-slash"></i> API Docs</a>
                <a href="/logout"><i class="bi bi-box-arrow-right"></i> Logout</a>
            </div>
        </header>
        
        <% if(locals.success_msg && success_msg.length > 0) { %>
            <div class="alert alert-success">
                <%= success_msg %>
            </div>
        <% } %>
        <% if(locals.error_msg && error_msg.length > 0) { %>
            <div class="alert alert-danger">
                <%= error_msg %>
            </div>
        <% } %>
        
        <div class="admin-links">
            <a href="/admin"><i class="bi bi-people"></i> User Management</a>
            <a href="/admin/logs"><i class="bi bi-journal-text"></i> System Logs</a>
            <a href="/site-map"><i class="bi bi-diagram-3"></i> Site Components</a>
        </div>
        
        <div class="admin-card">
            <h2><i class="bi bi-people-fill"></i> User Management</h2>
            <p>Manage user accounts and permissions.</p>
            
            <button class="action-btn" id="show-add-user"><i class="bi bi-person-plus"></i> Add New User</button>
            
            <div class="add-user-form" id="add-user-form">
                <h3>Add New User</h3>
                <form action="/admin/users/add" method="POST">
                    <div class="form-row">
                        <div class="form-group">
                            <label for="username">Username:</label>
                            <input type="text" id="username" name="username" required>
                        </div>
                        <div class="form-group">
                            <label for="email">Email:</label>
                            <input type="email" id="email" name="email" required>
                        </div>
                    </div>
                    <div class="form-row">
                        <div class="form-group">
                            <label for="password">Password:</label>
                            <input type="password" id="password" name="password" required>
                        </div>
                        <div class="form-group">
                            <label for="role">Role:</label>
                            <select id="role" name="role">
                                <option value="user">User</option>
                                <option value="admin">Administrator</option>
                            </select>
                        </div>
                    </div>
                    <div style="margin-top: 15px;">
                        <button type="submit" class="action-btn"><i class="bi bi-check-lg"></i> Create User</button>
                        <button type="button" class="action-btn" id="cancel-add-user" style="background-color: #777;"><i class="bi bi-x-lg"></i> Cancel</button>
                    </div>
                </form>
            </div>
            
            <table>
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Username</th>
                        <th>Email</th>
                        <th>Role</th>
                        <th>Admin</th>
                        <th>Created</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (users && users.length > 0) { %>
                        <% users.forEach(function(user) { %>
                            <tr>
                                <td><%= user.id %></td>
                                <td><%= user.username %></td>
                                <td><%= user.email %></td>
                                <td><%= user.role || 'user' %></td>
                                <td><%= user.isAdmin ? 'Yes' : 'No' %></td>
                                <td><%= new Date(user.created_at).toLocaleDateString() %></td>
                                <td>
                                    <a href="/user/<%= user.id %>" class="action-btn"><i class="bi bi-eye"></i> View</a>
                                    <button class="action-btn edit-btn edit-user-btn" 
                                        data-id="<%= user.id %>" 
                                        data-username="<%= user.username %>" 
                                        data-email="<%= user.email %>" 
                                        data-role="<%= user.role || 'user' %>" 
                                        data-admin="<%= user.isAdmin ? '1' : '0' %>">
                                        <i class="bi bi-pencil"></i> Edit
                                    </button>
                                    <a href="/admin/users/delete/<%= user.id %>" class="action-btn delete-btn" onclick="return confirm('Are you sure you want to delete this user?')"><i class="bi bi-trash"></i> Delete</a>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="7">No users found</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
        
        <div class="admin-card">
            <h2><i class="bi bi-gear-fill"></i> System Settings</h2>
            <p>Configure system-wide settings and preferences.</p>
            
            <div style="margin-top: 20px;">
                <form action="/admin/settings" method="POST">
                    <div style="display: flex; margin-bottom: 10px;">
                        <label style="width: 200px;">Debug Mode:</label>
                        <select name="debug_mode" style="background-color: #333; color: #fff; padding: 5px;">
                            <option value="1" <%= settings && settings.debug_mode === '1' ? 'selected' : '' %>>Enabled</option>
                            <option value="0" <%= settings && settings.debug_mode === '0' ? 'selected' : '' %>>Disabled</option>
                        </select>
                    </div>
                    
                    <div style="display: flex; margin-bottom: 10px;">
                        <label style="width: 200px;">Error Logging:</label>
                        <select name="error_logging" style="background-color: #333; color: #fff; padding: 5px;">
                            <option value="verbose" <%= settings && settings.error_logging === 'verbose' ? 'selected' : '' %>>Verbose</option>
                            <option value="standard" <%= settings && settings.error_logging === 'standard' ? 'selected' : '' %>>Standard</option>
                            <option value="minimal" <%= settings && settings.error_logging === 'minimal' ? 'selected' : '' %>>Minimal</option>
                        </select>
                    </div>
                    
                    <div style="display: flex; margin-bottom: 10px;">
                        <label style="width: 200px;">User Registration:</label>
                        <select name="user_registration" style="background-color: #333; color: #fff; padding: 5px;">
                            <option value="enabled" <%= settings && settings.user_registration === 'enabled' ? 'selected' : '' %>>Enabled</option>
                            <option value="disabled" <%= settings && settings.user_registration === 'disabled' ? 'selected' : '' %>>Disabled</option>
                            <option value="approval" <%= settings && settings.user_registration === 'approval' ? 'selected' : '' %>>Requires Admin Approval</option>
                        </select>
                    </div>
                    
                    <div style="display: flex; margin-bottom: 10px;">
                        <label style="width: 200px;">Default User Role:</label>
                        <select name="default_role" style="background-color: #333; color: #fff; padding: 5px;">
                            <option value="user" <%= settings && settings.default_role === 'user' ? 'selected' : '' %>>Regular User</option>
                            <option value="contributor" <%= settings && settings.default_role === 'contributor' ? 'selected' : '' %>>Contributor</option>
                            <option value="moderator" <%= settings && settings.default_role === 'moderator' ? 'selected' : '' %>>Moderator</option>
                        </select>
                    </div>
                    
                    <button type="submit" class="action-btn" id="save-settings"><i class="bi bi-save"></i> Save Settings</button>
                    <div class="settings-saved" id="settings-saved">Settings saved successfully!</div>
                </form>
            </div>
        </div>
    </div>
    
    <!-- User Edit Modal -->
    <div class="user-edit-modal" id="user-edit-modal">
        <div class="modal-content">
            <span class="close-modal" id="close-modal">&times;</span>
            <h2>Edit User</h2>
            <form action="/admin/users/edit" method="POST">
                <input type="hidden" id="edit-user-id" name="user_id">
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="edit-username">Username:</label>
                        <input type="text" id="edit-username" name="username" required>
                    </div>
                    <div class="form-group">
                        <label for="edit-email">Email:</label>
                        <input type="email" id="edit-email" name="email" required>
                    </div>
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="edit-role">Role:</label>
                        <select id="edit-role" name="role">
                            <option value="user">User</option>
                            <option value="admin">Administrator</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="edit-admin">Admin Status:</label>
                        <select id="edit-admin" name="isAdmin">
                            <option value="0">Not Admin</option>
                            <option value="1">Admin</option>
                        </select>
                    </div>
                </div>
                
                <div class="form-row">
                    <div class="form-group">
                        <label for="edit-password">New Password (leave blank to keep current):</label>
                        <input type="password" id="edit-password" name="password" placeholder="Enter new password">
                    </div>
                </div>
                
                <div style="margin-top: 15px;">
                    <button type="submit" class="action-btn"><i class="bi bi-check-lg"></i> Save Changes</button>
                </div>
            </form>
        </div>
    </div>

    <script>
        // Show/hide add user form
        document.getElementById('show-add-user').addEventListener('click', function() {
            document.getElementById('add-user-form').style.display = 'block';
            this.style.display = 'none';
        });
        
        document.getElementById('cancel-add-user').addEventListener('click', function() {
            document.getElementById('add-user-form').style.display = 'none';
            document.getElementById('show-add-user').style.display = 'inline-block';
        });
        
        // Show settings saved message
        document.getElementById('save-settings').addEventListener('click', function() {
            // This would normally happen after form submission, but for demo purposes
            document.getElementById('settings-saved').style.display = 'block';
            setTimeout(function() {
                document.getElementById('settings-saved').style.display = 'none';
            }, 3000);
        });
        
        // Modal functionality
        const modal = document.getElementById('user-edit-modal');
        const closeBtn = document.getElementById('close-modal');
        
        closeBtn.onclick = function() {
            modal.style.display = 'none';
        }
        
        window.onclick = function(event) {
            if (event.target == modal) {
                modal.style.display = 'none';
            }
        }
        
        // Set up edit buttons
        document.querySelectorAll('.edit-user-btn').forEach(button => {
            button.addEventListener('click', function() {
                const id = this.getAttribute('data-id');
                const username = this.getAttribute('data-username');
                const email = this.getAttribute('data-email');
                const role = this.getAttribute('data-role');
                const isAdmin = this.getAttribute('data-admin');
                
                document.getElementById('edit-user-id').value = id;
                document.getElementById('edit-username').value = username;
                document.getElementById('edit-email').value = email;
                document.getElementById('edit-role').value = role;
                document.getElementById('edit-admin').value = isAdmin;
                
                modal.style.display = 'block';
            });
        });
    </script>
</body>
</html> 