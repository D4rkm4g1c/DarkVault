<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - Network Tools</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .tool-card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        input[type="text"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 15px;
            background-color: #333;
            color: #fff;
            border: 1px solid #444;
            border-radius: 3px;
        }
        button {
            background-color: #00aaff;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 3px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0088cc;
        }
        .output {
            background-color: #333;
            border: 1px solid #444;
            border-radius: 3px;
            padding: 15px;
            margin-top: 20px;
            font-family: monospace;
            white-space: pre-wrap;
            max-height: 300px;
            overflow-y: auto;
        }
        .help-text {
            margin-top: 10px;
            font-size: 0.9em;
            color: #aaa;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Network Tools</h1>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <% if (user.isAdmin) { %>
                    <a href="/admin">Admin Panel</a>
                <% } %>
                <a href="/logout">Logout</a>
            </div>
        </header>
        
        <div class="tool-card">
            <h2>Ping Tool</h2>
            <p>Use this tool to check if a host is reachable on the network.</p>
            
            <form action="/ping" method="POST">
                <input type="text" name="host" placeholder="Enter hostname or IP address (e.g., google.com)" required>
                <button type="submit">Ping Host</button>
            </form>
            
            <div class="help-text">
                <p>Example: google.com, 192.168.1.1</p>
            </div>
            
            <% if (typeof output !== 'undefined') { %>
                <div class="output"><%= output %></div>
            <% } %>
        </div>
        
        <div class="tool-card">
            <h2>Other Network Tools</h2>
            <p>More network tools will be added soon...</p>
        </div>
    </div>
</body>
</html> 