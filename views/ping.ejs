<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - Network Tools</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .tool-card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        input[type="text"] {
            width: 100%;
            padding: 8px;
            margin-bottom: 15px;
            background-color: #333;
            color: #fff;
            border: 1px solid #444;
            border-radius: 3px;
        }
        button {
            background-color: #00aaff;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 3px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0088cc;
        }
        .output {
            background-color: #333;
            border: 1px solid #444;
            border-radius: 3px;
            padding: 15px;
            margin-top: 20px;
            font-family: monospace;
            white-space: pre-wrap;
            max-height: 300px;
            overflow-y: auto;
        }
        .help-text {
            margin-top: 10px;
            font-size: 0.9em;
            color: #aaa;
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>
    <%- include('partials/header') %>

    <div class="container mt-4">
        <h2>Network Ping Tool</h2>
        
        <div class="card mb-4">
            <div class="card-header">
                <h4>Ping Host</h4>
            </div>
            <div class="card-body">
                <form action="/ping" method="GET" class="mb-3">
                    <div class="form-group">
                        <label for="host-input">Host to ping:</label>
                        <input type="text" class="form-control" id="host-input" name="host" placeholder="Enter hostname or IP address" value="<%= typeof host !== 'undefined' ? host : '' %>">
                    </div>
                    <button type="submit" class="btn btn-primary mt-3">Ping</button>
                </form>
                
                <% if (typeof output !== 'undefined' && output) { %>
                    <div class="mt-4">
                        <h5>Output for: <%= host %></h5>
                        <pre class="bg-dark text-light p-3"><%= output %></pre>
                    </div>
                <% } %>
                
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="alert alert-danger mt-3">
                        Error: <%= error %>
                    </div>
                <% } %>
            </div>
        </div>
        
        <div class="mb-3">
            <a href="/" class="btn btn-secondary">Back to Dashboard</a>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html> 