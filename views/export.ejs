<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - Export Data</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2, h3 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        .export-data {
            background-color: #333;
            color: #fff;
            padding: 15px;
            border-radius: 3px;
            font-family: monospace;
            word-break: break-all;
            white-space: pre-wrap;
            max-height: 200px;
            overflow-y: auto;
        }
        .actions {
            margin-top: 20px;
            display: flex;
            gap: 10px;
        }
        .action-btn {
            background-color: #00aaff;
            color: white;
            text-decoration: none;
            border: none;
            padding: 8px 15px;
            border-radius: 3px;
            cursor: pointer;
            display: inline-block;
        }
        .action-btn:hover {
            background-color: #0088cc;
        }
        .instructions {
            margin-top: 15px;
            font-size: 0.9em;
            color: #aaa;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Data Export</h1>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/settings">Settings</a>
                <a href="/products">Products</a>
                <a href="/preview">URL Preview</a>
                <a href="/import">Import Products</a>
                <a href="/contact">Contact</a>
                <% if (user.isAdmin) { %>
                    <a href="/admin">Admin Panel</a>
                <% } %>
                <a href="/logout">Logout</a>
            </div>
        </header>
        
        <div class="card">
            <h2>Your Exported Data</h2>
            <p>The data below includes your account information and preferences. You can use this to backup your account or import it on another instance.</p>
            
            <h3>Serialized Data</h3>
            <div class="export-data"><%= exportData %></div>
            
            <div class="instructions">
                <p><strong>Instructions:</strong></p>
                <ol>
                    <li>Copy the data above.</li>
                    <li>Store it securely - it contains sensitive account information.</li>
                    <li>Use the "Import Data" function to restore this data.</li>
                </ol>
            </div>
            
            <div class="actions">
                <button class="action-btn" onclick="copyData()">Copy to Clipboard</button>
                <a href="/import-data" class="action-btn">Import Data</a>
            </div>
        </div>
    </div>
    
    <script>
        function copyData() {
            const exportData = document.querySelector('.export-data');
            const range = document.createRange();
            range.selectNode(exportData);
            window.getSelection().removeAllRanges();
            window.getSelection().addRange(range);
            document.execCommand('copy');
            window.getSelection().removeAllRanges();
            alert('Data copied to clipboard!');
        }
    </script>
</body>
</html> 