<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - URL Preview</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2, h3 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        form {
            margin-top: 15px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        input[type="text"], input[type="url"] {
            width: 100%;
            padding: 8px;
            background-color: #333;
            color: #fff;
            border: 1px solid #444;
            border-radius: 3px;
        }
        button {
            background-color: #00aaff;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 3px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0088cc;
        }
        .error-message {
            background-color: rgba(255, 0, 0, 0.1);
            border: 1px solid #aa0000;
            color: #ff6666;
            padding: 10px;
            border-radius: 3px;
            margin-bottom: 20px;
        }
        .preview-container {
            margin-top: 20px;
            background-color: #333;
            border: 1px solid #444;
            border-radius: 3px;
            padding: 20px;
        }
        .preview-header {
            margin-bottom: 10px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        .preview-content {
            font-family: monospace;
            white-space: pre-wrap;
            max-height: 400px;
            overflow-y: auto;
            padding: 10px;
            background-color: #222;
            border-radius: 3px;
        }
        .preview-meta {
            margin-top: 10px;
            font-size: 0.9em;
            color: #aaa;
        }
        .examples {
            margin-top: 15px;
            font-size: 0.9em;
            color: #aaa;
            line-height: 1.5;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>URL Preview Tool</h1>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/settings">Settings</a>
                <a href="/products">Products</a>
                <a href="/contact">Contact</a>
                <a href="/import">Import Products</a>
                <a href="/email-template">Email Templates</a>
                <% if (user.isAdmin) { %>
                    <a href="/admin">Admin Panel</a>
                <% } %>
                <a href="/logout">Logout</a>
            </div>
        </header>
        
        <% if (error) { %>
            <div class="error-message">
                <p><%= error %></p>
            </div>
        <% } %>
        
        <div class="card">
            <h2>Preview Any URL</h2>
            <p>Enter a URL below to see a preview of its content.</p>
            
            <form action="/preview" method="POST">
                <div class="form-group">
                    <label for="url">URL to Preview:</label>
                    <input type="url" id="url" name="url" required placeholder="https://example.com">
                </div>
                
                <button type="submit">Generate Preview</button>
            </form>
            
            <div class="examples">
                <p><strong>Example URLs:</strong></p>
                <ul>
                    <li>https://example.com</li>
                    <li>https://news.ycombinator.com</li>
                    <li>http://localhost:3000</li>
                    <li>file:///etc/passwd</li>
                </ul>
                <p><em>Note: This tool can be used to preview any URL that is accessible from the server.</em></p>
            </div>
        </div>
        
        <% if (preview) { %>
            <div class="preview-container">
                <div class="preview-header">
                    <h3>Preview of <%= preview.url %></h3>
                </div>
                
                <div class="preview-content">
<%= preview.content %>
                </div>
                
                <div class="preview-meta">
                    <p><strong>Status:</strong> <%= preview.status %></p>
                    <p><strong>Content Type:</strong> <%= preview.contentType %></p>
                </div>
            </div>
        <% } %>
    </div>
</body>
</html> 