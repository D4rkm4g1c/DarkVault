<%- include('partials/header') %>

<div class="container mt-5">
    <h2>File Manager</h2>
    <p class="lead">Upload and manage files in the system</p>
    
    <div class="row">
        <div class="col-md-6">
            <div class="card mb-4">
                <div class="card-header bg-primary text-white">
                    <h5 class="mb-0">Upload File</h5>
                </div>
                <div class="card-body">
                    <form action="/api/files/upload" method="POST" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="fileInput" class="form-label">Select File</label>
                            <input type="file" class="form-control" id="fileInput" name="file" required>
                        </div>
                        <div class="mb-3">
                            <label for="filename" class="form-label">Custom Filename (optional)</label>
                            <input type="text" class="form-control" id="filename" name="filename" placeholder="Leave blank to use original filename">
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary">Upload</button>
                        </div>
                    </form>
                </div>
                <div class="card-footer bg-light">
                    <small class="text-muted">Note: This file upload functionality may be vulnerable to specific attacks.</small>
                </div>
            </div>
        </div>
        
        <div class="col-md-6">
            <div class="card mb-4">
                <div class="card-header bg-dark text-white">
                    <h5 class="mb-0">View File</h5>
                </div>
                <div class="card-body">
                    <form action="/api/files/view" method="GET">
                        <div class="mb-3">
                            <label for="path" class="form-label">File Path</label>
                            <input type="text" class="form-control" id="path" name="path" required>
                        </div>
                        <div class="d-grid">
                            <button type="submit" class="btn btn-dark">View File</button>
                        </div>
                    </form>
                </div>
                <div class="card-footer bg-light">
                    <small class="text-danger">Warning: This endpoint is vulnerable to Path Traversal attacks!</small>
                </div>
            </div>
        </div>
    </div>
    
    <div class="card">
        <div class="card-header bg-secondary text-white">
            <h5 class="mb-0">Uploaded Files</h5>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Filename</th>
                            <th>Uploaded By</th>
                            <th>Upload Date</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (typeof files !== 'undefined' && files && files.length > 0) { %>
                            <% files.forEach(function(file) { %>
                                <tr>
                                    <td><%= file.id %></td>
                                    <td><%= file.filename %></td>
                                    <td><%= file.uploaded_by %></td>
                                    <td><%= new Date(file.upload_date).toLocaleString() %></td>
                                    <td>
                                        <a href="/api/files/download/<%= file.id %>" class="btn btn-sm btn-info">Download</a>
                                        <% if (user && (user.isAdmin || user.id === file.uploaded_by)) { %>
                                            <a href="/api/files/delete/<%= file.id %>" class="btn btn-sm btn-danger">Delete</a>
                                        <% } %>
                                    </td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="5" class="text-center">No files uploaded yet.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    
    <div class="alert alert-warning mt-4">
        <h5><i class="bi bi-exclamation-triangle"></i> Security Issues</h5>
        <ul>
            <li>The file viewer is vulnerable to <strong>Path Traversal</strong> attacks.</li>
            <li>Try using <code>../../../etc/passwd</code> or similar paths to access unauthorized files.</li>
            <li>The file upload feature may be vulnerable to malicious file uploads.</li>
        </ul>
    </div>
</div>

<%- include('partials/footer') %> 