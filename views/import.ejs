<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DarkVault - Import Products</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a;
            color: #f0f0f0;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 20px auto;
            padding: 20px;
        }
        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #444;
        }
        h1, h2, h3 {
            color: #00aaff;
        }
        .nav-links a {
            color: #00aaff;
            text-decoration: none;
            margin-left: 15px;
        }
        .nav-links a:hover {
            text-decoration: underline;
        }
        .card {
            background-color: #2a2a2a;
            border-radius: 5px;
            padding: 20px;
            margin-bottom: 20px;
        }
        form {
            margin-top: 15px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            margin-bottom: 5px;
        }
        textarea {
            width: 100%;
            height: 200px;
            padding: 8px;
            background-color: #333;
            color: #fff;
            border: 1px solid #444;
            border-radius: 3px;
            font-family: monospace;
        }
        button {
            background-color: #00aaff;
            color: white;
            border: none;
            padding: 8px 15px;
            border-radius: 3px;
            cursor: pointer;
        }
        button:hover {
            background-color: #0088cc;
        }
        .error-message {
            background-color: rgba(255, 0, 0, 0.1);
            border: 1px solid #aa0000;
            color: #ff6666;
            padding: 10px;
            border-radius: 3px;
            margin-bottom: 20px;
        }
        .success-message {
            background-color: rgba(0, 255, 0, 0.1);
            border: 1px solid #00aa00;
            color: #00ff00;
            padding: 10px;
            border-radius: 3px;
            margin-bottom: 20px;
        }
        .examples {
            margin-top: 15px;
            font-size: 0.9em;
            color: #aaa;
            line-height: 1.5;
        }
        pre {
            background-color: #222;
            padding: 10px;
            border-radius: 3px;
            overflow-x: auto;
            max-height: 300px;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1>Import Products from XML</h1>
            <div class="nav-links">
                <a href="/dashboard">Dashboard</a>
                <a href="/settings">Settings</a>
                <a href="/products">Products</a>
                <a href="/preview">URL Preview</a>
                <a href="/contact">Contact</a>
                <% if (user.isAdmin) { %>
                    <a href="/admin">Admin Panel</a>
                <% } %>
                <a href="/logout">Logout</a>
            </div>
        </header>
        
        <% if (error) { %>
            <div class="error-message">
                <p><%= error %></p>
            </div>
        <% } %>
        
        <% if (result) { %>
            <div class="success-message">
                <p>Products imported successfully!</p>
            </div>
        <% } %>
        
        <div class="card">
            <h2>Import Products</h2>
            <p>Paste your XML product data below to import it into the system.</p>
            
            <form action="/import" method="POST">
                <div class="form-group">
                    <label for="xml_data">XML Data:</label>
                    <textarea id="xml_data" name="xml_data" required placeholder="Paste your XML here..."></textarea>
                </div>
                
                <button type="submit">Import Products</button>
            </form>
            
            <div class="examples">
                <p><strong>Example XML Format:</strong></p>
                <pre>&lt;?xml version="1.0" encoding="UTF-8"?&gt;
&lt;products&gt;
  &lt;product&gt;
    &lt;name&gt;Gaming Mouse&lt;/name&gt;
    &lt;description&gt;High precision gaming mouse&lt;/description&gt;
    &lt;price&gt;59.99&lt;/price&gt;
    &lt;category&gt;Accessories&lt;/category&gt;
  &lt;/product&gt;
  &lt;product&gt;
    &lt;name&gt;Mechanical Keyboard&lt;/name&gt;
    &lt;description&gt;RGB mechanical gaming keyboard&lt;/description&gt;
    &lt;price&gt;129.99&lt;/price&gt;
    &lt;category&gt;Accessories&lt;/category&gt;
  &lt;/product&gt;
&lt;/products&gt;</pre>
                
                <p><em>Note: You can include as many products as needed in a single import.</em></p>
            </div>
        </div>
        
        <% if (result) { %>
            <div class="card">
                <h2>Import Result</h2>
                <pre><%= JSON.stringify(result, null, 2) %></pre>
            </div>
        <% } %>
    </div>
</body>
</html> 